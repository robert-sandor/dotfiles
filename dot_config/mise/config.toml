# always-on tools, pinned; project-specific tools live in each project's mise.toml
[tools]
node = "24.21.0"
"npm:yaml-language-server" = "1.24.0"
shellcheck = "0.11.0"
tombi = "1.5.5"
usage = "6.10.0"
